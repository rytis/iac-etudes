kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: efs-sc
provisioner: efs.csi.aws.com
# below options are needed as default efs config expects iam auth and tls comms
mountOptions:
  - tls
  - iam
parameters:
  provisioningMode: efs-ap
  fileSystemId: fs-061c2f8ac3c6bb990
  directoryPerms: "700"
